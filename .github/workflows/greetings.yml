name: Greetings

on: [pull_request, issues]

jobs:
  greeting:
    runs-on: ubuntu-latest
    steps:
    - uses: actions/first-interaction@v1
      with:
        repo-token: ${{ secrets.GITHUB_TOKEN }}
        issue-message: 'Hurray 🎉, your first issue! Thank you for your contribution ❤️.'
        pr-message: 'Hip Hip Hurray 🎉, your first pull request! Thank you so much for contributing 💕.'
